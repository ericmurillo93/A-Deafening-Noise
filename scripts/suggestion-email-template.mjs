const escapeHtml = (value) => String(value ?? "").replace(/[&<>"']/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[character]);

function safeUrl(value) {
  try { const url = new URL(value); return ["http:", "https:"].includes(url.protocol) ? escapeHtml(url.href) : ""; }
  catch { return ""; }
}

export function renderSuggestionDigest(displayName, suggestions) {
  const count = suggestions.length;
  const subject = count === 1 ? "A new concert matches your taste" : `${count} new concerts match your taste`;
  const cards = suggestions.map((suggestion) => {
    const eventUrl = safeUrl(suggestion.sourceUrl || suggestion.ticketUrl);
    const location = [suggestion.venue, suggestion.city, suggestion.country].filter(Boolean).map(escapeHtml).join(" · ");
    return `<tr><td style="padding:0 0 10px"><table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="border:1px solid #30343a;border-radius:8px;background:#111418"><tr><td style="width:82px;padding:18px 14px;border-right:1px solid #30343a;text-align:center;color:#60a5fa;font-size:13px;font-weight:800;line-height:1.35">${escapeHtml(suggestion.date)}</td><td style="padding:16px 18px"><div style="color:#f4f4f5;font-size:17px;font-weight:900;line-height:1.2;text-transform:uppercase;letter-spacing:.2px">${escapeHtml(suggestion.artist)}</div>${location ? `<div style="margin-top:7px;color:#a1a1aa;font-size:12px;line-height:1.5;text-transform:uppercase">${location}</div>` : ""}${eventUrl ? `<a href="${eventUrl}" style="display:inline-block;margin-top:10px;color:#60a5fa;font-size:12px;font-weight:700;text-decoration:none">View event&nbsp; →</a>` : ""}</td></tr></table></td></tr>`;
  }).join("");
  const html = `<!doctype html><html><head><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="color-scheme" content="dark"><title>${escapeHtml(subject)}</title></head><body style="margin:0;background:#09090b;color:#f4f4f5;font-family:Arial,Helvetica,sans-serif"><div style="display:none;max-height:0;overflow:hidden;color:transparent">Fresh concerts from artists you listen to.</div><table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="background:#09090b"><tr><td align="center" style="padding:28px 12px"><table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="max-width:620px"><tr><td style="padding:0 2px 22px"><div style="color:#f4f4f5;font-size:18px;font-weight:900;text-transform:uppercase;letter-spacing:.3px">A Deafening Noise</div><div style="margin-top:5px;color:#71717a;font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:1.8px">Concert discovery</div></td></tr><tr><td style="padding:26px 24px;border:1px solid #30343a;border-radius:8px;background:#15191e"><div style="color:#60a5fa;font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:1.2px">Your daily concert drop</div><h1 style="margin:10px 0 9px;color:#f4f4f5;font-size:30px;line-height:1.08;text-transform:uppercase">${count === 1 ? "One new show for you" : `${count} new shows for you`}</h1><p style="margin:0;color:#a1a1aa;font-size:14px;line-height:1.6">Hi ${escapeHtml(displayName || "there")}, today&apos;s discovery found ${count === 1 ? "a concert" : "concerts"} from artists already in your listening profile.</p></td></tr><tr><td style="height:16px"></td></tr>${cards}<tr><td align="center" style="padding:18px 0 10px"><a href="https://adeafeningnoise.com/suggestions" style="display:inline-block;border-radius:8px;background:#2563eb;color:#fff;padding:14px 22px;font-size:13px;font-weight:800;text-decoration:none;text-transform:uppercase">Review suggestions&nbsp; →</a></td></tr><tr><td style="padding:22px 8px 0;border-top:1px solid #27272a;color:#71717a;font-size:11px;line-height:1.6;text-align:center">You receive this daily digest only when new matches are found. <a href="https://adeafeningnoise.com/profile" style="color:#a1a1aa">Manage email preferences</a>.</td></tr></table></td></tr></table></body></html>`;
  const text = `A Deafening Noise\n\nHi ${displayName || "there"},\n\n${subject}:\n\n${suggestions.map((suggestion) => `- ${suggestion.artist} — ${suggestion.date}${suggestion.venue ? ` — ${suggestion.venue}` : ""}${suggestion.sourceUrl ? `\n  ${suggestion.sourceUrl}` : ""}`).join("\n")}\n\nReview suggestions: https://adeafeningnoise.com/suggestions\nManage email preferences: https://adeafeningnoise.com/profile`;
  return { subject, html, text };
}
